---
description: Do not exfiltrate secrets when using ValueSignal capture tools
alwaysApply: true
---

When using ValueSignal MCP tools:

- Never send API keys, passwords, JWTs, or `.env` contents in `valuesignal_capture_turn`
- Prefer summaries over full file dumps for large artifacts
- Tell the user capture is optional and they control what is submitted
